<Window x:Class="WinKnightUI.TempGraphPopup"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Temperature Monitor"
        Height="450" Width="800"
        WindowStartupLocation="CenterScreen"
        Background="#F0F2F5"
        WindowStyle="None"
        AllowsTransparency="True"
        MouseLeftButtonDown="Window_MouseLeftButtonDown">

    <Window.Resources>
        <SolidColorBrush x:Key="CardBackground" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="PrimaryText" Color="#1E1E1E"/>
        <SolidColorBrush x:Key="SecondaryText" Color="#666666"/>
        <Style x:Key="PopupCard" TargetType="Border">
            <Setter Property="Background" Value="{StaticResource CardBackground}"/>
            <Setter Property="CornerRadius" Value="10"/>
            <Setter Property="Padding" Value="20"/>
            <Setter Property="BorderBrush" Value="#DCDCDC"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>
        <Style x:Key="PopupChromeButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource PrimaryText}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#DDDDDD"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource PrimaryText}"/>
        </Style>
    </Window.Resources>

    <Border Style="{StaticResource PopupCard}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" Margin="0,0,0,10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="Temperature Monitor" FontSize="20" FontWeight="Bold" VerticalAlignment="Center"/>
                <Button Grid.Column="1" Width="30" Content="—" Style="{StaticResource PopupChromeButton}" Click="Minimize_Click"/>
                <Button Grid.Column="2" Width="30" Content="▢" Style="{StaticResource PopupChromeButton}" Margin="6,0" Click="MaxRestore_Click"/>
                <Button Grid.Column="3" Width="30" Content="×" Style="{StaticResource PopupChromeButton}" Click="Close_Click"/>
            </Grid>

            <Grid Grid.Row="1" Background="#F0F0F0" Margin="0,10,0,0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Canvas x:Name="GraphCanvas" Grid.Row="0" Margin="10">
                </Canvas>

                <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" Margin="10">
                    <Ellipse Width="10" Height="10" Fill="Red" Margin="0,0,5,0"/>
                    <TextBlock Text="CPU" Margin="0,0,15,0"/>
                    <Ellipse Width="10" Height="10" Fill="Blue" Margin="0,0,5,0"/>
                    <TextBlock Text="GPU" Margin="0,0,15,0"/>
                    <Ellipse Width="10" Height="10" Fill="Green" Margin="0,0,5,0"/>
                    <TextBlock Text="System" Margin="0,0,15,0"/>
                </StackPanel>
            </Grid>
        </Grid>
    </Border>
</Window>