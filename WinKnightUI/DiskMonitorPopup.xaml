<Window x:Class="WinKnightUI.DiskMonitorPopup"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Disk Monitor"
        Height="400" Width="600"
        MinHeight="350" MinWidth="550"
        WindowStartupLocation="CenterScreen"
        Background="Transparent"
        WindowStyle="None"
        AllowsTransparency="True"
        MouseLeftButtonDown="Window_MouseLeftButtonDown">

    <Window.Resources>
        <SolidColorBrush x:Key="BackgroundBrush" Color="#0B1A31"/>
        <SolidColorBrush x:Key="CardBackground" Color="#182A41"/>
        <SolidColorBrush x:Key="ItemBackground" Color="#0F1F35"/>
        <SolidColorBrush x:Key="PrimaryText" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="SecondaryText" Color="#8796A6"/>
        <SolidColorBrush x:Key="AccentBlue" Color="#3498DB"/>
        <SolidColorBrush x:Key="AccentRed" Color="#E74C3C"/>
        <SolidColorBrush x:Key="AccentGreen" Color="#2ECC71"/>
        <SolidColorBrush x:Key="WarningYellow" Color="#F1C40F"/>
        <SolidColorBrush x:Key="GridLine" Color="#1E3A5F"/>

        <Style x:Key="PopupCard" TargetType="Border">
            <Setter Property="Background" Value="{StaticResource CardBackground}"/>
            <Setter Property="CornerRadius" Value="15"/>
            <Setter Property="Padding" Value="25"/>
            <Setter Property="BorderBrush" Value="{StaticResource AccentBlue}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect BlurRadius="20" ShadowDepth="5" Opacity="0.3" Color="#000000"/>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="PopupChromeButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource PrimaryText}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#22FFFFFF"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource PrimaryText}"/>
        </Style>

        <Style x:Key="ListBoxItemStyle" TargetType="ListBoxItem">
            <Setter Property="Margin" Value="0,5"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border x:Name="border" Background="{StaticResource ItemBackground}" Padding="15" CornerRadius="10"
                                BorderThickness="1" BorderBrush="Transparent">
                            <ContentPresenter HorizontalAlignment="Stretch" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#1E3A5F"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource AccentBlue}"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#25476B"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource AccentBlue}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="DiskProgressBar" TargetType="ProgressBar">
            <Setter Property="Height" Value="10"/>
            <Setter Property="Margin" Value="0,5,0,0"/>
            <Setter Property="Background" Value="#1E3A5F"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Foreground" Value="{StaticResource AccentBlue}"/>
            <Style.Triggers>
            </Style.Triggers>
        </Style>

        <Style x:Key="StatusTextStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="{StaticResource AccentGreen}"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding Status}" Value="Warning">
                    <Setter Property="Foreground" Value="{StaticResource WarningYellow}"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding Status}" Value="Critical">
                    <Setter Property="Foreground" Value="{StaticResource AccentRed}"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding Status}" Value="Bad">
                    <Setter Property="Foreground" Value="{StaticResource AccentRed}"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>

    </Window.Resources>

    <Border Style="{StaticResource PopupCard}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="💽" FontSize="24" Margin="0,0,10,0"/>
                    <TextBlock Text="Disk Health Monitor" FontSize="22" FontWeight="Bold" VerticalAlignment="Center"/>
                </StackPanel>

                <Button Grid.Column="2" Width="30" Height="30" Content="—" Style="{StaticResource PopupChromeButton}" Click="Minimize_Click"/>
                <Button Grid.Column="3" Width="30" Height="30" Content="▢" Style="{StaticResource PopupChromeButton}" Margin="6,0" Click="MaxRestore_Click"/>
                <Button Grid.Column="4" Width="30" Height="30" Content="×" Style="{StaticResource PopupChromeButton}" Click="Close_Click"/>
            </Grid>

            <Border Grid.Row="1" Height="1" Margin="0,15" Background="{StaticResource GridLine}" CornerRadius="0.5"/>

            <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
                <ListBox x:Name="DriveList"
                         Background="Transparent"
                         BorderThickness="0"
                         ItemContainerStyle="{StaticResource ListBoxItemStyle}"
                         HorizontalContentAlignment="Stretch">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Grid Margin="0,5">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="150"/>
                                </Grid.ColumnDefinitions>

                                <StackPanel Grid.Column="0" VerticalAlignment="Center">
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="{Binding Name, Mode=OneWay}" FontWeight="Bold" FontSize="16"/>
                                        <TextBlock Text="{Binding ModelName, StringFormat=' ({0})', Mode=OneWay}" FontSize="12" Foreground="{StaticResource SecondaryText}" VerticalAlignment="Center" Margin="5,0,0,0"/>
                                    </StackPanel>
                                    <StackPanel Orientation="Horizontal" Margin="0,2,0,0">
                                        <TextBlock Text="Type: " FontSize="12" FontWeight="SemiBold" Foreground="{StaticResource SecondaryText}"/>
                                        <TextBlock Text="{Binding DiskType, Mode=OneWay}" FontSize="12" Foreground="{StaticResource SecondaryText}" Margin="5,0,0,0"/>
                                    </StackPanel>
                                    <TextBlock Text="{Binding Status, Mode=OneWay}" Style="{StaticResource StatusTextStyle}" Margin="0,5,0,0"/>
                                </StackPanel>

                                <StackPanel Grid.Column="1" HorizontalAlignment="Right" VerticalAlignment="Center">
                                    <TextBlock Text="{Binding UsedSpaceGb, StringFormat='{}Used: {0:F1} GB', Mode=OneWay}" FontSize="12" HorizontalAlignment="Right"/>
                                    <TextBlock Text="{Binding TotalSpaceGb, StringFormat='{}Total: {0:F1} GB', Mode=OneWay}" FontSize="12" HorizontalAlignment="Right" Foreground="{StaticResource SecondaryText}"/>
                                    <ProgressBar Value="{Binding UsagePercentage, Mode=OneWay}" Style="{StaticResource DiskProgressBar}"/>
                                </StackPanel>
                            </Grid>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </ScrollViewer>
        </Grid>
    </Border>
</Window>